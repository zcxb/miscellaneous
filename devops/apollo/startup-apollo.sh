# logs stored in /opt, so apollo need permission to write logs.

# edit startup.sh in /config directory
# open jvm options, decrese -Xms and -Xmx
# change SERVER_PORT, LOG_DIR

# run startup.sh
./startup.sh